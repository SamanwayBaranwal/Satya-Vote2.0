# Copy to .env. Get a free WalletConnect projectId at https://cloud.walletconnect.com
VITE_WALLETCONNECT_PROJECT_ID="your_walletconnect_project_id"

# Optional: override the deployed contract address (otherwise read from src/contracts/addresses.json)
VITE_CONTRACT_ADDRESS=""